// import express from "express";
// import {
//   getCartItems,
//   getCartItem,
//   addCartItem,
//   updateCartItem,
//   deleteCartItem,
// } from "../controllers/cart.js";

// const router = express.Router();

// router.get("/", getCartItems); // Get all cart items
// router.get("/:id", getCartItem); // Get a single cart item by ID
// router.post("/", addCartItem); // Add a new item to the cart
// router.put("/:id", updateCartItem); // Update a cart item by ID
// router.delete("/:id", deleteCartItem); // Delete a cart item by ID

// export default router;
